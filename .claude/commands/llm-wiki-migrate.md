---
description: Bring an existing wiki/notes collection into a bundle (one-time)
argument-hint: <path-or-repo> [bundle]
---

Run `/llm-wiki-migrate` exactly as AGENTS.md defines it.

1. Read `AGENTS.md` in full, unless you already have in this session.
2. Read `workflows/migrate.md`, then `workflows/create-bundle.md`, then `workflows/ingest.md` in full before doing anything — the procedure lives there,
   not in this file.
3. Everything in AGENTS.md applies — trust boundary, bundle resolution,
   Git discipline, the Rules. This file adds nothing to them; it only
   makes the command typeable in Claude Code.

Arguments: $ARGUMENTS
