---
description: Change a bundle's content language
argument-hint: <bundle> <language>
---

Run `/llm-wiki-set-bundle-language` exactly as AGENTS.md defines it.

1. Read `AGENTS.md` in full, unless you already have in this session.
2. The procedure is short enough to live in AGENTS.md itself: follow
   the section "Changing a bundle's content language".
3. Everything in AGENTS.md applies — trust boundary, bundle resolution,
   Git discipline, the Rules. This file adds nothing to them; it only
   makes the command typeable in Claude Code.

Arguments: $ARGUMENTS
